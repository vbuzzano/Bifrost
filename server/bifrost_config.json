{
  "comment": "Bifrost Server Configuration. 'network.port' sets the default TCP port (UDP discovery is always port+1); 'python main.py --port N' overrides it for one-off runs.",
  "network": {
    "port": 7890
  },
  "mouse": {
    "hz": 50,
    "hz_drag": 15,
    "speed": 1,
    "delta_max": 80
  },
  "curve": {
    "linear": 2.0,
    "ratio": 0.5
  },
  "keys": {
    "toggle": "scroll_lock",
    "emergency": "pause",
    "kill_modifier": "ctrl"
  },
  "debug": {
    "enabled": true,
    "print_events": true
  }
}
